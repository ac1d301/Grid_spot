# Server Configuration
PORT=5001

# MongoDB Configuration
# Replace <username>, <password>, <cluster-url> with your MongoDB Atlas credentials
MONGODB_URI=mongodb+srv://saiitejaa4853:sai@f1cluster.dazivni.mongodb.net/

# JWT Configuration
# Replace this with a secure secret key in production
JWT_SECRET=secret

# CORS Configuration
# In production, replace with your frontend domain
CORS_ORIGIN=http://localhost:8080,https://grid-spot-web.vercel.app